version: '3'

vars:
  DEFAULT_GFX_ARCH: '{{default "gfx1030;gfx1100;gfx1103" .GFX_ARCH}}'
  DEFAULT_HSA_OVERRIDE_GFX_VERSION: '{{default "11.0.0" .HSA_OVERRIDE_GFX_VERSION}}'

tasks:
  run-active-listener:
    desc: Start the active-listener workstation dictation CLI
    dotenv: ['.env']
    dir: .
    cmds:
      - uv sync --all-extras --all-groups --project packages/active-listener
      - uv run --project packages/active-listener active-listener --config-path packages/active-listener/config.yaml {{.CLI_ARGS}}

  install-active-listener-service:
    desc: Install or update the active-listener user systemd service
    dir: .
    cmds:
      - uv sync --all-extras --all-groups --project packages/active-listener
      - |
        python3 - <<'PY'
        from pathlib import Path
        import shutil

        repo_root = Path.cwd()
        unit_template = repo_root / "packages/active-listener/systemd/active-listener.service.in"
        unit_destination = Path.home() / ".config/systemd/user/active-listener.service"
        config_destination = Path.home() / ".config/eavesdrop/active-listener.yaml"
        prompt_destination = Path.home() / ".config/eavesdrop/active-listener.rewrite.system.md"

        package_dir = repo_root / "packages/active-listener"
        active_listener_bin = repo_root / ".venv/bin/active-listener"
        if not active_listener_bin.exists():
          raise SystemExit(f"active-listener entrypoint not found at {active_listener_bin}")

        unit_text = unit_template.read_text()
        unit_text = unit_text.replace("@REPO_ROOT@", str(repo_root))
        unit_text = unit_text.replace("@ACTIVE_LISTENER_BIN@", str(active_listener_bin))
        unit_destination.parent.mkdir(parents=True, exist_ok=True)
        unit_destination.write_text(unit_text)

        config_destination.parent.mkdir(parents=True, exist_ok=True)
        if not config_destination.exists():
          for source in (
            package_dir / "config.yaml",
            package_dir / "config.sample.yaml",
          ):
            if source.exists():
              shutil.copy2(source, config_destination)
              break
          else:
            raise SystemExit("No active-listener config source found to seed the XDG config path")

        print(f"Installed {unit_destination}")
        print(f"Using config path {config_destination}")
        if prompt_destination.exists():
          print(f"Prompt override path {prompt_destination}")
        PY
      - systemctl --user daemon-reload
      - systemctl --user enable --now active-listener.service
      - |
        deadline=$((SECONDS + 60))
        while [ "$SECONDS" -lt "$deadline" ]; do
          if systemctl --user is-active --quiet active-listener.service; then
            sleep 10
            if systemctl --user is-active --quiet active-listener.service; then
              systemctl --user status active-listener.service --no-pager
              exit 0
            fi
          fi
          sleep 2
        done
        systemctl --user status active-listener.service --no-pager
        journalctl --user -u active-listener.service -n 100 --no-pager
        exit 1

  uninstall-active-listener-service:
    desc: Stop, disable, and remove the active-listener user systemd service
    dir: .
    cmds:
      - systemctl --user disable --now active-listener.service || true
      - rm -f "$HOME/.config/systemd/user/active-listener.service"
      - systemctl --user daemon-reload
      - |
        if systemctl --user is-enabled active-listener.service >/dev/null 2>&1; then
          echo "active-listener.service is still enabled"
          exit 1
        fi
      - |
        if systemctl --user is-active --quiet active-listener.service; then
          systemctl --user status active-listener.service --no-pager
          exit 1
        fi

  prepare-active-listener-ui-gnome:
    desc: Install active-listener-ui-gnome dependencies
    dir: packages/active-listener-ui-gnome
    cmds:
      - pnpm install --frozen-lockfile

  build-active-listener-ui-gnome:
    desc: Build the active-listener-ui-gnome extension bundle
    deps: [prepare-active-listener-ui-gnome]
    dir: packages/active-listener-ui-gnome
    cmds:
      - pnpm run build

  install-active-listener-ui-gnome:
    desc: Build and install the active-listener-ui-gnome GNOME Shell extension
    deps: [build-active-listener-ui-gnome]
    dir: packages/active-listener-ui-gnome
    cmds:
      - gnome-extensions install --force .out/eavesdrop@shyndman.ca.zip

  test-active-listener-ui-gnome:
    desc: Launch the active-listener-ui-gnome nested Wayland GNOME Shell test session
    deps: [prepare-active-listener-ui-gnome]
    dir: packages/active-listener-ui-gnome
    cmds:
      - |
        journalctl --user --follow --output cat --since now \
          | grep --line-buffered 'Active Listener' &
        journal_tail_pid=$!
        trap 'kill "$journal_tail_pid"' EXIT
        pnpm run wayland:test

  prepare-client-lib:
    desc: Install client library dependencies
    dir: packages/client
    cmds:
      - uv sync --all-extras --all-groups

  transcribe-file:
    desc: Transcribe a local audio file (set audio_file=/path/to/audio.wav)
    deps: [prepare-client-lib]
    dir: packages/client
    vars:
      host: '{{default "localhost" .host}}'
      port: '{{default "9090" .port}}'
    requires:
      vars: [audio_file]
    cmds:
      - uv run python -m eavesdrop.client.transcribe_file_cli --audio-file "{{.audio_file}}" --host "{{.host}}" --port {{.port}} {{.CLI_ARGS}}

  run-mic-client:
    desc: Start microphone transcription client
    deps: [prepare-client-lib]
    dir: packages/client
    vars:
      host: '{{default "localhost" .host}}'
      port: '{{default "9090" .port}}'
      audio_device: '{{default "default" .audio_device}}'
    cmds:
      - uv run python examples/simple_transcriber.py --server "{{.host}}:{{.port}}" --audio-device "{{.audio_device}}" --model "distil-small.en" {{.CLI_ARGS}}

  prepare-server:
    desc: Install server dependencies
    dir: packages/server
    cmds:
      - uv sync --all-extras --all-groups

  list-gpus:
    desc: Print GPU names as reported by PyTorch on the current machine
    cmds:
      - uv run --no-project "$(command -v python3)" -c 'import json, torch; print(json.dumps([torch.cuda.get_device_name(i) for i in range(torch.cuda.device_count())]))'

  run-server:
    desc: Start the transcription server with local config
    deps: [prepare-server]
    dir: packages/server
    cmds:
      - uv run eavesdrop-server --config config.yaml

  build-rocm-base-image:
    # Verified ROCm build settings on AMD Framework 13 (Ryzen 7840U / Radeon 780M).
    desc: Build the ROCm base image used by the server image
    dir: packages
    sources:
      - server/docker/Dockerfile.rocm-base
    vars:
      ROCM_BASE_IMAGE: '{{default "eavesdrop-rocm-base" .ROCM_BASE_IMAGE}}'
      GFX_ARCH: '{{.DEFAULT_GFX_ARCH}}'
      HSA_OVERRIDE_GFX_VERSION: '{{.DEFAULT_HSA_OVERRIDE_GFX_VERSION}}'
      GFX_ARCH_LABEL: 'io.eavesdrop.gfx_arch'
      HSA_OVERRIDE_GFX_VERSION_LABEL: 'io.eavesdrop.hsa_override_gfx_version'
    status:
      - docker image inspect {{.ROCM_BASE_IMAGE}} >/dev/null 2>&1
      - python3 -c 'import json, subprocess, sys; image = json.loads(subprocess.check_output(["docker", "image", "inspect", "{{.ROCM_BASE_IMAGE}}"], text=True))[0]; labels = image["Config"].get("Labels") or {}; sys.exit(0 if labels.get("{{.GFX_ARCH_LABEL}}") == "{{.GFX_ARCH}}" else 1)'
      - python3 -c 'import json, subprocess, sys; image = json.loads(subprocess.check_output(["docker", "image", "inspect", "{{.ROCM_BASE_IMAGE}}"], text=True))[0]; labels = image["Config"].get("Labels") or {}; sys.exit(0 if labels.get("{{.HSA_OVERRIDE_GFX_VERSION_LABEL}}") == "{{.HSA_OVERRIDE_GFX_VERSION}}" else 1)'
    cmds:
      - echo "GFX_ARCH = {{.GFX_ARCH}}"
      - echo "HSA_OVERRIDE_GFX_VERSION = {{.HSA_OVERRIDE_GFX_VERSION}}"
      - docker build --pull --no-cache --progress=plain -f server/docker/Dockerfile.rocm-base --build-arg GFX_ARCH='{{.GFX_ARCH}}' --build-arg HSA_OVERRIDE_GFX_VERSION='{{.HSA_OVERRIDE_GFX_VERSION}}' --label {{.GFX_ARCH_LABEL}}='{{.GFX_ARCH}}' --label {{.HSA_OVERRIDE_GFX_VERSION_LABEL}}='{{.HSA_OVERRIDE_GFX_VERSION}}' -t {{.ROCM_BASE_IMAGE}} .

  build-server-image:
    desc: Build the final server image
    deps: [build-rocm-base-image]
    dir: packages
    sources:
      - common/pyproject.toml
      - common/src/**/*
      - wire/pyproject.toml
      - wire/src/**/*
      - server/docker/Dockerfile
      - server/docker/entrypoint.sh
      - server/pyproject.toml
      - server/src/**/*
      - server/.dockerignore
      - server/uv.lock
    vars:
      SERVER_IMAGE: '{{default "ghcr.io/shyndman/lmnop-eavesdrop:latest" .SERVER_IMAGE}}'
      ROCM_BASE_IMAGE: '{{default "eavesdrop-rocm-base" .ROCM_BASE_IMAGE}}'
      GFX_ARCH: '{{.DEFAULT_GFX_ARCH}}'
      HSA_OVERRIDE_GFX_VERSION: '{{.DEFAULT_HSA_OVERRIDE_GFX_VERSION}}'
      BASE_IMAGE_ID_LABEL: 'io.eavesdrop.rocm_base_image_id'
      GFX_ARCH_LABEL: 'io.eavesdrop.gfx_arch'
      HSA_OVERRIDE_GFX_VERSION_LABEL: 'io.eavesdrop.hsa_override_gfx_version'
    status:
      - docker image inspect {{.ROCM_BASE_IMAGE}} >/dev/null 2>&1
      - python3 -c 'import json, subprocess, sys; image = json.loads(subprocess.check_output(["docker", "image", "inspect", "{{.ROCM_BASE_IMAGE}}"], text=True))[0]; labels = image["Config"].get("Labels") or {}; sys.exit(0 if labels.get("{{.GFX_ARCH_LABEL}}") == "{{.GFX_ARCH}}" else 1)'
      - python3 -c 'import json, subprocess, sys; image = json.loads(subprocess.check_output(["docker", "image", "inspect", "{{.ROCM_BASE_IMAGE}}"], text=True))[0]; labels = image["Config"].get("Labels") or {}; sys.exit(0 if labels.get("{{.HSA_OVERRIDE_GFX_VERSION_LABEL}}") == "{{.HSA_OVERRIDE_GFX_VERSION}}" else 1)'
      - docker image inspect {{.SERVER_IMAGE}} >/dev/null 2>&1
      - python3 -c 'import json, subprocess, sys; base = json.loads(subprocess.check_output(["docker", "image", "inspect", "{{.ROCM_BASE_IMAGE}}"], text=True))[0]["Id"]; server = json.loads(subprocess.check_output(["docker", "image", "inspect", "{{.SERVER_IMAGE}}"], text=True))[0]; labels = server["Config"].get("Labels") or {}; sys.exit(0 if labels.get("{{.BASE_IMAGE_ID_LABEL}}") == base else 1)'
      - python3 -c 'import json, subprocess, sys; image = json.loads(subprocess.check_output(["docker", "image", "inspect", "{{.SERVER_IMAGE}}"], text=True))[0]; labels = image["Config"].get("Labels") or {}; sys.exit(0 if labels.get("{{.GFX_ARCH_LABEL}}") == "{{.GFX_ARCH}}" else 1)'
      - python3 -c 'import json, subprocess, sys; image = json.loads(subprocess.check_output(["docker", "image", "inspect", "{{.SERVER_IMAGE}}"], text=True))[0]; labels = image["Config"].get("Labels") or {}; sys.exit(0 if labels.get("{{.HSA_OVERRIDE_GFX_VERSION_LABEL}}") == "{{.HSA_OVERRIDE_GFX_VERSION}}" else 1)'
    cmds:
      - echo "GFX_ARCH = {{.GFX_ARCH}}"
      - echo "HSA_OVERRIDE_GFX_VERSION = {{.HSA_OVERRIDE_GFX_VERSION}}"
      - |
        BASE_IMAGE_ID="$(docker image inspect {{.ROCM_BASE_IMAGE}} --format '{{"{{"}}.Id{{"}}"}}')"
        docker build --progress=plain -f server/docker/Dockerfile --build-arg BASE_IMAGE={{.ROCM_BASE_IMAGE}} --label {{.BASE_IMAGE_ID_LABEL}}="$BASE_IMAGE_ID" --label {{.GFX_ARCH_LABEL}}='{{.GFX_ARCH}}' --label {{.HSA_OVERRIDE_GFX_VERSION_LABEL}}='{{.HSA_OVERRIDE_GFX_VERSION}}' -t {{.SERVER_IMAGE}} .

  run-server-image:
    # Verified ROCm runtime flags on AMD Framework 13 (Ryzen 7840U / Radeon 780M).
    desc: Run the server Docker image with ROCm runtime flags
    deps: [build-server-image]
    dir: .
    vars:
      SERVER_IMAGE: '{{default "ghcr.io/shyndman/lmnop-eavesdrop:latest" .SERVER_IMAGE}}'
      HSA_OVERRIDE_GFX_VERSION: '{{.DEFAULT_HSA_OVERRIDE_GFX_VERSION}}'
      LOG_LEVEL: '{{default "INFO" .LOG_LEVEL}}'
    cmds:
      - docker run --interactive --tty
        --device /dev/kfd
        --device /dev/dri
        --security-opt seccomp=unconfined
        --rm
        --publish '9090:9090'
        --name eavesdrop
        --env HSA_OVERRIDE_GFX_VERSION='{{.HSA_OVERRIDE_GFX_VERSION}}'
        --env LOG_LEVEL='{{.LOG_LEVEL}}'
        --volume "$HOME/.cache/lmnop/eavesdrop-models:/root/.cache/eavesdrop"
        --volume "$PWD/packages/server/config.yaml:/app/server/config.yaml:ro"
        --env EAVESDROP_CONFIG=/app/server/config.yaml
        {{.SERVER_IMAGE}} -m eavesdrop.server

  run-server-image-persistent:
    # Verified ROCm runtime flags on AMD Framework 13 (Ryzen 7840U / Radeon 780M).
    desc: Run the server Docker image as a restart-always background container
    deps: [build-server-image]
    dir: .
    vars:
      SERVER_IMAGE: '{{default "ghcr.io/shyndman/lmnop-eavesdrop:latest" .SERVER_IMAGE}}'
      HSA_OVERRIDE_GFX_VERSION: '{{.DEFAULT_HSA_OVERRIDE_GFX_VERSION}}'
      LOG_LEVEL: '{{default "INFO" .LOG_LEVEL}}'
    cmds:
      - |
        if docker inspect -f '{{"{{"}}.State.Running{{"}}"}}' eavesdrop >/dev/null 2>&1 && [ "$(docker inspect -f '{{"{{"}}.State.Running{{"}}"}}' eavesdrop)" = "true" ]; then
          docker stop eavesdrop
        fi
      - docker rm eavesdrop || true
      - docker run --detach
        --device /dev/kfd
        --device /dev/dri
        --security-opt seccomp=unconfined
        --restart always
        --publish '9090:9090'
        --name eavesdrop
        --env HSA_OVERRIDE_GFX_VERSION='{{.HSA_OVERRIDE_GFX_VERSION}}'
        --env LOG_LEVEL='{{.LOG_LEVEL}}'
        --volume "$HOME/.cache/lmnop/eavesdrop-models:/root/.cache/eavesdrop"
        --volume "$PWD/packages/server/config.yaml:/app/server/config.yaml:ro"
        --env EAVESDROP_CONFIG=/app/server/config.yaml
        {{.SERVER_IMAGE}} -m eavesdrop.server

  stop-server-image:
    desc: Stop the running server container if needed, then remove it
    cmds:
      - |
        if docker inspect -f '{{"{{"}}.State.Running{{"}}"}}' eavesdrop >/dev/null 2>&1 && [ "$(docker inspect -f '{{"{{"}}.State.Running{{"}}"}}' eavesdrop)" = "true" ]; then
          docker stop eavesdrop
        fi
      - docker rm eavesdrop || true
