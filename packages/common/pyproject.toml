[project]
name = "eavesdrop-common"
version = "0.0.0"
description = "Add your description here"
authors = [{name = "shyndman", email = "scotty.hyndman@gmail.com"}]
requires-python = ">=3.12.3"
dependencies = [
    "numpy<2",
    "pydantic>=2",
    "rich>=15,<16",
    "structlog>=25.4.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.urls]
repository = "https://github.com/shyndman/eavesdrop"
"Bug Tracker" = "https://github.com/shyndman/eavesdrop/issues"

[tool.hatch.build.targets.wheel]
packages = ["src/eavesdrop"]

[tool.hatch.build.targets.wheel.force-include]
"src/eavesdrop/common/py.typed" = "eavesdrop/common/py.typed"

[tool.basedpyright]
include = ["src", "tests"]
ignore = ["**/.venv"]
defineConstant = {DEBUG = true}
reportMissingImports = "error"
deprecateTypingAliases = true
reportDeprecated = "information"
pythonPlatform = "Linux"

[tool.ruff]
line-length = 100
target-version = "py312"
indent-width = 2

[tool.ruff.lint.flake8-tidy-imports]
# Disallow all relative imports.
ban-relative-imports = "all"

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
ignore = []

[tool.ruff.lint.isort]
known-first-party = ["eavesdrop"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "auto"

[tool.uv.sources]
rich = { git = "https://github.com/shyndman/rich" }

[dependency-groups]
dev = [
    "basedpyright>=1.35.0",
]
